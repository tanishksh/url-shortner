<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Home Page</title>
</head>

<body style="margin:0; padding: 10%; box-sizing: border-box;font-family: Tahoma;">

    <h1>URL Shortener</h1>

    <div>
        <form method="post" action="/url">
            <label for="">Enter the original URL :</label>
            <input type="text" name="url" placeholder="https://example.com" id="">
            <button type="submit">Generate</button>
        </form>
    </div>

    <% if (locals.id) { %>
        <p>
        <h1>URL Generated : </h1> http://localhost:8001/url/<%= id %>
            </p>
            <% } %>

                <% if (locals.urls) { %>
                    <table>
                        <thead>
                            <th>S. No</th>
                            <th>Short ID</th>
                            <th>Redirects</th>
                            <th>Clicks</th>
                        </thead>
                        <tbody>
                            <% urls.forEach((url,i)=> { %>
                                <tr>
                                    <td style="margin: 10px; padding: 10px;">
                                        <%= i+1 %>
                                    </td>
                                    <td>
                                        <%= url.shortId %>
                                    </td>
                                    <td>
                                        <%= url.redirectURL %>
                                    </td>
                                    <td>
                                        <%= url.visitHistory.length %>
                                    </td>
                                </tr>
                                <% }) %>
                        </tbody>
                    </table>
                    <% } %>


</body>

</html>